
//Set icon item
setIcon(<twilightforest:magic_map>)

//Set title String
setTitle("Magic Map")

//Set description String
//setDescription("Make a 2x2 pool of water, surround it with mushrooms, throw a diamond in, jump in, and craft a magic map.")
setDescription("Throw a diamond in a 2x2 pool of water surrounded by mushrooms, jump in, and craft a magic map.")

//Add a parent advancement. The syntax is "modid:folder/path/to/the/advancement/then/filename"
addParent("myconesia:diamond")

//Set the toast notification in the top right corner of the screen to not show up when this advancement is achieved.
setShowToast(true)

//Set the advancement to not send a chat message when it is achieved.
setAnnounceToChat(false)

//Set the position
setPos(-115,91)

//Hide the connection lines, so I can have a pretty shape
//hideLines()
alwaysVisible()

//Adds criteria named "hasStickOfTruth" with the trigger type "minecraft:inventory_changed". This function returns the criteria as an object
criteria = addCriteria("hasMagicMap", "minecraft:inventory_changed")

//Adds the required item for the criteria. This is the item that needs to be in the player inventory.
criteria.addItem(<twilightforest:magic_map>)